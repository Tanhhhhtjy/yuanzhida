<view class="container">
  <the-input-group inputItems="{{inputItems}}" bind:input="onInput"> </the-input-group>
  <the-button text="{{captchaText}}" bind:the_tap="get_code"></the-button>
  <the-button text="注册" bind:the_tap="submit"></the-button>
</view>